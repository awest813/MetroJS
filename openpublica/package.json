{
  "name": "openpublica",
  "version": "0.1.0",
  "description": "OpenPublica — a GPLv3 browser-native 3D city-builder",
  "license": "GPL-3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "models": "node scripts/build-models.mjs",
    "test": "npm --prefix .. test -- --coverage=false"
  },
  "dependencies": {
    "@babylonjs/core": "^9.0.0",
    "@babylonjs/loaders": "^9.4.1",
    "alea": "^1.0.1",
    "simplex-noise": "^4.0.3"
  },
  "devDependencies": {
    "typescript": "^6.0.0",
    "vite": "^6.3.6"
  }
}
